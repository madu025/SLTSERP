# 2026-08-03

## Decimal/UUID migration — TypeScript remediation complete

- `npx prisma validate`: PASS
- `npx tsc --noEmit`: clean (was 286 errors across ~54 files; errors archived at `.agent/artifacts/tsc-errors.txt`)
- Fix pattern applied everywhere: `Number(decimalField)` at math, comparison, and DTO boundaries; removed explicit numeric callback annotations conflicting with Prisma Decimal rows; converted `_sum`/`?? 0` unions at the source.
- Batches fixed: GIS (5), finance (16), project (13 incl. slt/contractor/invoice), inventory (11), core/AI/fleet/helpdesk/lib/components (~9).
- Notable fixes:
  - `stock-request.service.ts` had `reqItem.receivedQty.toNumber() = x` (invalid assignment) → in-memory cast pattern.
  - `report.service.ts` had `existing.total_amount.toNumber() += x` (invalid) → plain numeric accumulation.
  - `helpdesk/assets/page.tsx` now imports the AssetList local `ITAsset` type and coerces `purchaseCost` to number.
- Live Supabase audit (`node .agent/artifacts/qa_datatype_audit.js`):
  - PASS: all id columns UUID; zero float/double columns remaining.
  - 3 "bad FK" flags are false positives: `Contractor.registrationFeePaid` (boolean flag), `GISRoadData.fid`/`wayFbid` (legacy OSM feature IDs, not relational FKs).
  - Text status/role columns are advisory warnings only — schema intentionally uses String over DB enums.

## Contractor Team Data Import

- Created `prisma/seed-contractor-teams.js` — idempotent seed script for 204-row spreadsheet data
- **Data imported:**
  - 188 Contractors (2 pre-existing + 186 from spreadsheet, each named after Team Leader)
  - 203 Teams (189 with Ishamp/SLT codes, all 203 linked to OPMC via RTO)
  - 371 Members (198 Team Leaders, 173 Team Members)
  - 12 "SLTS Direct" teams for Direct Team rows (19 members)
- Teams spread across 40 RTOs (R-AD through R-WT)
- Logic: Team Leader name becomes Contractor name; Direct Teams go under "SLTS Direct" contractor
- Ishamp login codes stored as `sltCode` on ContractorTeam
- Team members linked via `teamId` with designation ("Team Leader" / "Team Member")
- NIC and contact numbers captured for all members

## IT Asset Laptop Import (NDJSON)

- Source: `prisma/data/it-asset-assignments.ndjson` (184 rows, 142 laptops, 112 employees)
- Importer: `scripts/import-it-asset-assignments.ts` (run via `npx tsx`), idempotent keyed on serialNumber
- **Semantics:** userOrder 1 = current custodian (verified against agent telemetry: PF57GRSD order-1 emp 883 matched lastSeenEmployeeNumber); userOrder 2+ = past custodians logged as AssetHandoverLog history with staggered dates (higher order = older)
- **Results:** 141 assets created + 1 updated (PF57GRSD kept SLT-AGENT-IT number), 29 staff auto-created + logins provisioned via `ensureUserAccountForStaff`, 184 handover logs, 0 failures
- Brand inferred from serial prefix: Lenovo 85 (PF*/T*NRCX/etc.), HP 55 (5CD*/CND* etc.), Acer 2; asset numbers `SLT-LAP-1001+` continuing sequence
- Dirty rows: `TEM-HR AND ADMIN` (PF3K9WM1) and null employeeNo (PF5TWLG1) imported with `pendingAssignmentReview: true`; past-user dirty employeeNos (NIC `INC199300203809`, nulls) still logged with names
- AssetHandoverLog.performedById needs real User UUID (no SYSTEM user exists) — used first SUPER_ADMIN
