# Memory Log: 2026-07-09

## Accomplishments
1. **Service Architecture Reorganization**:
   - Reorganized flat service files under `src/services/` into modular directories: `gis/` and `project/`.
   - Maintained re-exports at the root `src/services/` (e.g. `gis.service.ts`, `project.service.ts`) for backwards-compatibility.
   - Unified duplicate `PaymentVoucher` service implementations under `src/services/finance/payment-voucher.service.ts`.
2. **Petty Cash Imprest Module**:
   - Added schema models (`PettyCashAccount`, `PettyCashVoucher`, `PettyCashReimbursement`).
   - Created `PettyCashService` handling account setup, vouchers draft/approve/reject, and bulk replenishment requests.
   - Integrated double-entry logic into `LedgerService` (`logPettyCashExpense`, `logPettyCashReimbursement`).
   - Created API routes for the petty cash sub-module.
3. **OSP & Finance Gaps Fixed**:
   - **Costing Shortage Bug**: Implemented a fallback to the standard cost price of the item when an inventory shortage is detected.
   - **Contractor Payable Accrual**: Added `logContractorAccrual` to automatically accrue contractor costs upon SOD completion.
   - **Payment Voucher Cap Check**: Added checks during creation and updates to prevent payment vouchers from exceeding invoice totals.
   - **Contractor Pricing Lower Boundary**: Added boundary fallback paying the contractor the lowest tier amount when distance is below the min tier distance.
4. **Development Skill Documentation**:
   - Updated `.agent/skills/development-workflow/SKILL.md` to reflect the latest unified facade directory structure and new schema models.
5. **Geopackage Data Migration to PostgreSQL**:
   - Created `scripts/upload-gpkg-to-postgres.ts` to bulk migrate 249,482 rows of road network coordinates from SQLite `.gpkg` file to Supabase PostgreSQL database.
   - Enhanced `GISRoadData` model to store pre-calculated `minLat`, `maxLat`, `minLon`, `maxLon` spatial bounds.
   - Refactored `GISGeoPackageService.ts` to query PostgreSQL instead of the local sqlite db, improving serverless cold-start efficiency and resolving Vercel deployment file-size limitations.
6. **Financial Business Validations & Sidebar Audit**:
   - Added validations in `PettyCashService.ts` to block negative/zero voucher amounts, block vouchers exceeding the imprest limit, and prevent duplicate pending reimbursement requests.
   - Refactored `completeReimbursement` to increment the petty cash balance by the replenishment amount rather than resetting it, avoiding leaking funds when expenses are incurred during the pending window.
   - Enforced strict state transitions inside `PaymentVoucherService.ts` to protect terminal states (`PAID`, `REJECTED`, `CANCELLED`) and validate submission flows.
   - Audited the Sidebar menu to add Petty Cash under `Finance & Accounts` and granted access to `ROLE_GROUPS.OFFICE_ADMINS` (Site Office Staff) to view it.
7. **Invoice and Payment Voucher General Ledger Integration**:
   - Implemented `logInvoiceIssuance` and `logPaymentVoucherPayment` in `LedgerService.ts` to post double-entry entries for invoices and payments.
   - Hooked `logInvoiceIssuance` into `project-invoice.service.ts` when transitioning status to `ISSUED`.
   - Hooked `logPaymentVoucherPayment` into `payment-voucher.service.ts` when transitioning status to `PAID`.
   - Written and executed `src/scratch/test_ledger_flow.ts` to simulate the full transactional cycle of OSP completed job -> Accrual -> Invoice -> AP -> PV Payment -> Bank cash outflow, confirming perfect accounting balance.
8. **SLT BOM Bulk Invoice Import & Client Billing Sync (Corrected)**:
   - Implemented `BOMInvoiceService.ts` to parse SLT BOM Excel & CSV rows on-the-fly, extract unique Service Order Numbers (`soNum`), match them to DB records, and update their PAT status to `PAT_PASSED` / `hoPatStatus = 'APPROVED'`.
   - Groups the matched connections by OPMC, retrieves flat rates from `SODRevenueConfig`, and generates a single **Client Invoice** (`ProjectInvoice` type `CLIENT`) to be submitted to SLT (representing the recognized Project Revenue) without raw sheet bloat.
   - Updated the UI in `src/app/invoices/page.tsx` to add "Import BOM Sheet (Client Billing & PAT Sync)" supporting direct CSV upload and a draggable "Sync to ERP" bookmarklet.
   - Wrote and executed `src/scratch/test_bom_csv.ts` to verify CSV parser updates, client invoicing, and warnings.
9. **BOM Sheets & Sync List View**:
    - Created the dashboard list page `src/app/invoices/bom-sheets/page.tsx` under the path `/invoices/bom-sheets` and registered it in `src/config/sidebar-menu.ts` as "BOM Sheets".
    - Integrates the original SLT portal BOM path (e.g. `BOM/R-AD/...`) inside the invoice `referenceNumber`.
    - Displays a direct **"Download BOM"** action button next to each imported invoice in the list, allowing users to click and download the original CSV file directly from the SLT portal using their active browser session.
10. **SLT Portal BOM List Registry Sync & View**:
    - Implemented a two-tab interface toggling between "ERP Invoices" (our paginated invoicing screen) and a live replica "SLT Portal BOM List" matching the SLT portal layout.
    - Created the backend API endpoint `/api/invoices/slt-registry` to dynamically fetch the live BOM sheet list from `https://serviceportal.slt.lk/iShamp/contr/dynamic_load?x=ftthbomload&z=SLTS` using a saved session cookie, parsing the HTML table on the fly and falling back to a cached JSON file if offline.
    - Created the endpoint `/api/invoices/slt-registry/download-sync` to fetch and sync any BOM sheet CSV directly in the backend using the saved session cookie, creating client invoices in one click without client-side manual downloads.
    - Integrated SLT Session Cookie input configuration field in the dashboard UI for seamless authentication.
