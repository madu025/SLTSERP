# Daily Notes: 2026-07-13

## Presentation Slide Enhancement
- **Created Deployment & Costing Slide**: Created `src/app/presentation/slides/Slide16.tsx` detailing comparative infrastructure pricing between self-hosted replicated DB nodes (AWS Lightsail) and a managed database replication setup (Supabase Pro + Replica) labeled as Option A and Option B:
  - **Option A (Self-Hosted Replicated Stack)**: ~$77.00/mo (VPS 1 App Node: $24, VPS 2 Primary Write Master DB: $24, VPS 3 Read Replica DB: $24, DO Spaces: $5).
  - **Option B (Managed Cloud Stack)**: ~$74.00/mo (VPS 1 App Droplet: $24, Supabase Pro Primary DB: $25, Supabase Read Replica: $25, Supabase Storage: $0).
  - **Shared Operational Costs**: Added Gemini Pro API (~$15.00/mo), Email (~$0.00/mo), and Domain (~$2.00/mo).
- **Documentation Cleanup**: Deleted `scratch/trash_backup` to clear legacy context, and completely updated `README.md` and `docs/SYSTEM_DOCUMENTATION.md` to reflect the new architecture, explicit SLT API endpoints, and detailed **Stores & Inventory Management Module** workflows (FIFO, virtual swaps, wastage logging, ROP alerts, multi-store auditing, and valuation costing reports).
- **Stores Manager Guide**: Created a workspace guide [STORES_WORKFLOW_GUIDE.md](file:///d:/MyProject/SLTSERP/docs/STORES_WORKFLOW_GUIDE.md) and artifact `stores_workflow_guide.md` containing a detailed English and Sinhala breakdown of the stores module, explicitly detailing the 4 material classification channels (SOD Materials, OSP Project Materials, General Materials, and Serial-tracked assets like Laptops & Printers).
- **Three-Stage PAT Verification & Invoicing Logic**: Refactored logic inside `src/services/sod/sod.invoicing.service.ts`, `sod.lifecycle.service.ts`, and `sod.sync.service.ts` to require all three gates (OPMC, HO, and SLT HQ) to be `PAT_PASSED` before a Service Order becomes `isInvoicable` and is added to the Billing BOM. Resolves TypeScript 'any' type linter issues in the interface.
- **HO Rejected PAT Sync Integration**: Implemented `syncHoRejectedResults` in `sod.sync.service.ts` to fetch rejected PATs globally, update the status of affected orders to `PAT_REJECTED` and `isInvoicable: false`, and trigger automatic rollback of material usage and financial transactions. Registered it in `sod-sync.worker.ts` under periodic global syncs.
- **Updated fetchHORejected signature**: Modified `fetchHORejected` in `slt-api.service.ts` to accept an optional `dateStr` parameter, matching the structure of `https://serviceportal.slt.lk/iShamp/contr/dynamic_load.php?x=patreject&y=[DATE]&con=SLTS`.
- **Updated Slides List**: Overhauled [Slide1.tsx](file:///d:/MyProject/SLTSERP/src/app/presentation/slides/Slide1.tsx) to list all 8 developed ERP modules matching standard navigation naming, registered `Slide16` as Slide 15 in `src/app/presentation/slides/index.ts`, updated [Slide5.tsx](file:///d:/MyProject/SLTSERP/src/app/presentation/slides/Slide5.tsx) to reflect the new **3-Stage PAT Gate** requirement, updated [Slide2.tsx](file:///d:/MyProject/SLTSERP/src/app/presentation/slides/Slide2.tsx) with system-specific digital leap comparisons, updated [Slide3.tsx](file:///d:/MyProject/SLTSERP/src/app/presentation/slides/Slide3.tsx) to match the new **3-Stage PAT Gate** pipeline, and updated [Slide4.tsx](file:///d:/MyProject/SLTSERP/src/app/presentation/slides/Slide4.tsx) mock table columns to use real SLT schema keys (`SO_NUM`, `VOICENUMBER`, `S_TYPE`, `ORDER_TYPE`).
- **Seamless Real-Time Data Refresh**: Addressed Route Caching Drift by declaring `export const dynamic = 'force-dynamic'` in `/api/service-orders` and `/api/service-orders/pat` routes, and implemented client-side cache busting (appending timestamp parameter `_t=${Date.now()}` and `no-store` headers) in `service-orders/page.tsx` and `service-orders/pat/page.tsx` fetches to ensure updates load seamlessly without requiring manual browser reloads.
- **Fixed Hydration Mismatch Crash**: Resolved client-side React hydration boundary error in `src/app/service-orders/page.tsx` caused by reading `localStorage` (via `sod_show_metrics` and `sod_sheet_mode`) directly inside initial `useState` declarations. Moved the read operations into a client-side `useEffect` hook to prevent differences between server and client markup during hydration.
- **SLT-Direct Sourced Materials (ONT, STB, Phone) Tracking**: Documented the special sourcing flow for active equipment in [STORES_WORKFLOW_GUIDE.md](file:///d:/MyProject/SLTSERP/docs/STORES_WORKFLOW_GUIDE.md) and [SYSTEM_DOCUMENTATION.md](file:///d:/MyProject/SLTSERP/docs/SYSTEM_DOCUMENTATION.md), detailing how these items bypass physical stores, are issued directly to the Area Coordinator, and are tracked using single serial entries or bulk Excel uploads.
- **Collected CPE Recovery & Monthly Handback Tracking**: Added support for tracking old CPE devices (routers, STBs, phones) retrieved by field technicians during device installations.
  - **Prisma Schema Split Update**: Added the `CollectedCPE` model to `prisma/schema/service-order.prisma` and relation keys in `prisma/schema/contractor.prisma`.
  - **Prisma DB Sync**: Ran `npx prisma db push` to synchronize model structures to the Supabase database.
  - **Backend APIs & Logic**: Added validation schema in `service-order.schema.ts`, transaction upsert inside `sod/index.ts` to record collected devices on completion, and built `/api/service-orders/cpe` to handle retrieval and bulk monthly handback handovers.
  - **Frontend UI**: Integrated a new "CPE Recovery" tab inside the guided completion wizard (`OrderActionModal.tsx` and `useOrderAction.ts`) allowing dynamic row entries of recovered devices.
  - **Documentation**: Documented workflow and specifications in `STORES_WORKFLOW_GUIDE.md` and `SYSTEM_DOCUMENTATION.md`.
- **Verification**: Verified using `npx tsc --noEmit` which completed successfully with 0 errors.

