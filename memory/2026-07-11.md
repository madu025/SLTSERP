# Daily Notes: 2026-07-11

## Contractor Invoices & SOD Isolation Refactoring

- **Added Key Fields to PATCH API Route**: Updated `/api/invoices` (`src/app/api/invoices/route.ts`) to accept and save `connectionTitle`, `agreementNumber`, and `projectNumber` in the database.
- **Invoice Edit Key Data Dialog**: Implemented an "Edit Key Data" action button and form dialog in `src/app/invoices/page.tsx`. Managers can now edit connection title, agreement number, and project number. These fields are updated in the database.
- **Removed OSP Project Module Links**:
  - Removed "Import BOM Sheet" action button, states, and upload dialog from `src/app/invoices/page.tsx`.
  - Decoupled `ProjectInvoice` from the details API endpoint (`src/app/api/invoices/[id]/details/route.ts`), keeping it strictly for SOD contractor claims.
  - Decoupled `ProjectInvoice` and OSP project code parsing from `src/lib/excel-invoice.ts`.
  - Removed `BOM Sheets` and `Delay Sheets` navigation links from the Sidebar menu (`src/config/sidebar-menu.ts`).
- **Verification**: Verified using `npx tsc --noEmit` which completed with 0 compilation errors.
