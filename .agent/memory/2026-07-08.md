# Session Memory - 2026-07-08

## Tasks Completed
* Audited and resolved GPS Route Map bugs:
  1. **Locations not saving**: Synced planned cables coordinates and recalculated lengths dynamically in `translateend` handler of `GISMapView.tsx` when a pole or closure is dragged.
  2. **Feed Point not marked/persisted**: Initialized `feedPointCoord` state from the database-loaded active route on page load. Styled the Feed Point (closure index `0`) with a green `📍` Feed Point styling and assigned `getPlannedFeedPointPopupHtml` popup for detail view.
  3. **Logical algorithm issues**:
     - Fixed backwards layer mapping in save API `/api/gis/auto-plan/save/route.ts` where `DOME` mapped to FDP and `TERMINAL` mapped to Joint.
     - Preserved dragged custom closures during replanning triggered by splitter ratio or base origin changes.
     - Corrected ref violations by passing ref objects (`mapRef` and `autoPlanDrawRef`) instead of `.current` inside JSX to `<GISSidebarPanel>`.
  4. **National map styling**: Added `'FEED'` layer name match handling in `NationalInfraMap.tsx` to render it as a distinct green Feed Point marker.
* **Custom Route Re-generation (Project `cmr3b0q2a00f5si6khytg417j`)**:
  - Deleted the old invalid layout plan.
  - Improved planning algorithm constraints in `src/services/gis/GISRoadNetwork.ts` to block diagonal major road centerline crossings at intersections, collapse medians (right shoulder) to outer shoulders (left shoulder) on oneway highways to prevent cables running down the middle of roads, and enabled snapping/routing inside roundabout center islands. Also demoted major road priorities and applied a large score penalty (0.8) to major road DP candidates in `src/services/gis/GISCandidateScoring.ts` to prevent placing DPs/poles on main multi-lane highways (lanes > 2), forcing them onto inner side streets instead.
  - Implemented pole-line routing for drop cables longer than 35m in `src/services/GISAutoPlanService.ts`. If the customer drop length exceeds 35m, instead of flying diagonally across private properties, the drop cable snaps to the closest point on the distribution cable (pole line) and runs along the pole line back to the DP, keeping the final direct aerial span short.
  - Fixed duplicate distance label rendering in `src/components/gis/GISMapView.tsx` by removing the total segment length text along the line, leaving the red midpoint labels as the single source of span distance display on the map.
  - Resolved overlapping double route rendering on the map: updated `src/components/gis/GISMapView.tsx` to skip rendering the static planned route from the database (`gisRoutes`) when the interactive planning draft (`autoPlanData`) is active.
  - Optimized pole placement constraints in `src/services/gis/GISPolePlacement.ts`: increased the duplicate merging threshold from 5m to 12m and increased the sharp bend angle threshold from 15 to 35 degrees to prune redundant poles on straight spans and minor curves.
  - Implemented a recursive cable pruning algorithm (`pruneRedundantTails`) in `src/services/GISAutoPlanService.ts` to identify and remove short dead-end distribution cable segments and connector loops (length < 15m) that do not terminate at a closure, cleaning up roundabout slip roads and junction entry artifacts.
  - Switched the Prim's Minimum Spanning Tree (MST) topology calculation in `src/services/GISAutoPlanService.ts` from straight-line (Euclidean) distance to actual road routing (Dijkstra) distance. This prevents geographically close closures on opposite sides of a blocked highway crossing from forming physical loops on the map.
  - Implemented spatial proximity road connection logic in `src/services/gis/GISRoadNetwork.ts` to bridge parallel or adjacent disconnected road segments (like dual-carriageway lanes or service roads separated by medians) at all points within 30 meters. This permits Dijkstra to schedule direct perpendicular crossings across major highways rather than forcing cables to route around the entire length of the highway block.
  - Redesigned the pole placement model in `src/services/gis/GISPolePlacement.ts` to place a support pole (or closure) at *every single coordinate vertex* (endpoints and bends > 5 degrees) of the simplified cable path, plus spacing intervals. It then maps the cable coordinates array exactly to the sequence of these pole/closure locations, eliminating all floating "phantom nodes" (dots without labels) on the map for full diagnosis.
  - Implemented parallel carriageway centerline intersection merging in `src/services/gis/GISRoadNetwork.ts` by clustering nearby centerline nodes (within 35 meters) of topological junctions, excluding any nodes that belong to a roundabout segment. This resolves dual-carriageway OSM topology gaps while preventing intersection-crossing shortcuts from leaping across roundabout center islands.
  - Treated roundabout junction nodes (where normal roads meet roundabout ring ways) as topological intersections in `src/services/gis/GISRoadNetwork.ts` to allow side-street shoulders to merge with major highway shoulders at roundabouts, resolving direct-path routing blocks.
  - Modified the graph `addEdge` helper in `src/services/gis/GISRoadNetwork.ts` to preserve the minimum weight if an edge already exists. This prevents later spatial proximity crossing loops from overwriting low-weight intersection edges with high-weight penalties.
  - Implemented roundabout center island crossing protection in `src/services/gis/GISRoadNetwork.ts` by blocking any spatial proximity connections where the segment midpoint or intermediate check points intersect the roundabout's center island circle. This forces cables to route around the roundabout ring road rather than shortcutting straight across the center.
  - Standardized the definition of major roads (highway types: 'motorway', 'trunk', 'primary', 'secondary', or lanes >= 4) across the entire codebase to prevent drop cables from crossing main secondary roads in Sri Lanka.
  - Expanded candidate DP generation in `src/services/gis/GISCandidateScoring.ts` to include road shoulders within 50 meters of the planning polygon boundary, allowing parallel shoulders of highway ways to host DPs even if the tight drawn polygon boundary cuts them off.
  - Implemented the **Project Director IR Material Audit & Tracking** module:
    - Seeded 34 Cables, 12 Closures, and 5 Patch Panels into the inventory items master database.
    - Implemented a unified ledger service `src/services/project-ir-ledger.service.ts` to audit SLT Inspection Receipt (IR) balances (Received, Issued, Leftover, Wasted, Returned, Transferred).
    - Created REST API routes for ledger queries, issues, returns, wastage, and P2P transfers under `/api/projects/`.
    - Developed a premium, feature-rich admin dashboard at `/projects/dashboards/ir-audit` containing KPI cards, live ledger logs, and dynamic action modals.
    - Updated project transfers, returns, wastage, and SLT return modal forms to query and display only items that have an actual leftover/in-hand balance in the source project context. This automatically locks logistics operations to realistic balance sheets.
    - Designed and implemented a chronological **IR Transaction Audit Timeline** (Slide-out panel) that traces the complete lifecycle of issues, returns, wastage, transfers, and receipts for any selected Inspection Receipt batch.
    - Updated `src/config/sidebar-menu.ts` to register the new dashboard link in the projects navigation group.
  - Replaced arbitrary pole planning heights with SLT standard pole heights (5.6m, 6.7m, 8.0m) based on geographical layout:
    - **8.0m** for road crossings, intersections, and roundabout junctions.
    - **6.7m** for main/major road shoulders (motorway, trunk, primary, secondary ways).
    - **5.6m** for standard distribution poles along minor/residential streets.
  - Implemented dynamic FDP (closure) snapping and mounting rules:
    - **Standard Coverage DPs** are snapped to their nearest planned pole coordinates, placing FDP symbols exactly on top of distribution poles along the cable route.
    - **Dedicated MDU DPs** skip road shoulder snapping and remain mounted directly on the commercial building centroid/structure itself.
  - Fixed a road shoulder routing pollution bug in `src/services/GISAutoPlanService.ts` by ensuring that temporary candidate pathfinding probes in Prim's MST do not commit shoulder side choices to the global `usedRoadShoulders` state. Commits are now locked only to the final selected tree edges, eliminating parallel cables on opposite sides of the road.
  - Aligned the **IR Material Audit Dashboard UI styling** with the general dashboard design system (PM and Logistics pages). Wrapped the content in a `max-w-7xl mx-auto w-full` container, replaced hardcoded dark mode colors (`slate-900`/`text-white`) with dynamic theme classes (`bg-background`, `bg-card`, `border-border`, `text-foreground`), and matched the stats card design and layout structure.
  - Updated GIS route fetching endpoints (`src/app/api/projects/[id]/gis/[routeId]/route.ts` and `src/app/api/gis/route.ts`) to return child assets (poles, closures, and cable segments) sorted sequentially by their index numbers (`poleNumber`, `closureNumber`, `segmentNumber`) to ensure they load on the map in the correct topological order.
  - Resolved critical functional gaps in the project ledger (`src/services/project-ir-ledger.service.ts`):
    - Enforced boundary validations to block negative/zero quantities (`INVALID_QUANTITY`).
    - Added target project lifecycle checks to prevent transactions on `COMPLETED` or `CANCELLED` projects (`INVALID_PROJECT_STATUS`).
    - Implemented transaction-safe leftover stock checks inside database transactions to prevent concurrent race conditions.
    - Automated the wastage rate calculation: `wastageRate = (wastedQty / receivedQty) * 100`.
    - Integrated BOQ budget limits constraint validation: cumulative issued quantity cannot exceed 120% of the planned BOQ quantity (`BOQ_LIMIT_EXCEEDED`).
    - Added contractor selector attribution support in project issues and wastage logs.
    - Integrated a real-time flashing alert indicator for wastage rates exceeding 5% and a pinging dot animation for over-budget BOQ variance (exceeding 100%).
    - Implemented contractor selection dropdowns in the Issue Material and Report Wastage modals, and required the SLT Gatepass Reference Number input in the Return to SLT modal.
  - Implemented the **AI Model Training & Deviation Analyzer** framework (`src/services/gis/GISAITrainingService.ts` and `/api/gis/ai-training`):
    - Created the deviation calculation service using the Haversine formula to compute average geographic snap displacements for matching poles across routes.
    - Added comprehensive support for the four distinct OSP map phases: AI Planned (`PLANNED`), QField Surveyed (`FIELD_CHANGE`), Before PAT Survey (`BEFORE_PAT`), and As-Built (`AS_BUILT`).
    - Configured the upload wizard UI (`src/components/gis/GISUpload.tsx`) and route controllers to accept and store the `'BEFORE_PAT'` design phase.
    - Integrated a dedicated dashboard tab in the IR Audit page (`src/app/projects/dashboards/ir-audit/page.tsx`) showing dataset count, model accuracy percentage, average snap displacement, height corrections, and ML training dataset statuses (`OPTIMIZED` vs. `IN_TRAINING` vs. `DEVIATED`) with a dedicated Before PAT column in the matrix.








  - Split candidate DP placement on major highways in `src/services/gis/GISCandidateScoring.ts` by generating separate candidate locations on the left shoulder and right shoulder instead of a single centerline point, creating separate serving zones for both sides.
  - Exempted DP-to-DP spacing checks (30m rule) in `src/services/gis/GISCandidateScoring.ts` if two candidates are separated by a major road centerline. This permits placing parallel shoulder DPs on opposite sides of a highway to serve customers locally.
  - Fixed a coordinate projection bug in `src/services/GISAutoPlanService.ts` by setting initial MDU dedicated DP coordinates to their original building centroids rather than snapped centerline nodes. This preserves the building's side-of-the-road direction during shoulder snapping.
  - Fixed a drop cable length calculation bug in `src/services/GISAutoPlanService.ts` by passing the raw geographic distance to `getDropCablePath` instead of the penalized candidate search distance.
  - Implemented a `cleanPath` path-level loop cleaner in `src/services/GISAutoPlanService.ts` to collapse any sub-8.0m snap-drift or U-turn loops (`A -> B -> A`).
  - Ran a script to query Overpass API, generated a fresh plan with the new Feeder Point at `(7.4871, 80.3663)`.
  - Saved the new plan to the database (78 poles, 30 closures, and 173 cables) and recalculated the BOQ.

## Verification
* TypeScript compile checks (`npm run test:types`) passed with 0 errors.
* Route recreation completed successfully in place in the database.
