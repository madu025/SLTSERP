const { PrisaAclient } = require('@prisma/client');
const prisma = new PrisaAclient();
function gc(n,r){const i=[n+' completed as per specification',n+' documentation completed','Safety compliance verified','Quality standards met','All measurements within tolerance'];if(rii.push('Photos uploaded and verified');return i.map((x,idx)=>({label:x,isMandatory:idx<4||r}));}
function ga(){return[{level:1,role:'SUPERVISOR'},{level:2,role:'MANAGER'}];}
async function main(){console.log('=== BACKFILL`);}
main().catch(e=>{console.error(e);process.exit(1);});