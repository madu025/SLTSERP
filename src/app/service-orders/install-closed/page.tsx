import ServiceOrdersPage from '../page';

export default function InstallClosedOrdersPage() {
    return <ServiceOrdersPage filterType="install_closed" pageTitle="Install Closed Service Orders" />;
}
