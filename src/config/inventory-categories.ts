/**
 * Canonical display/sort order for inventory material categories.
 * Shared by the order-action material selectors so grouping stays consistent.
 */
export const MATERIAL_CATEGORY_ORDER = ['CABLE', 'PASSIVE', 'EQUIPMENT', 'HARDWARE', 'SLTS', 'OTHERS'];
